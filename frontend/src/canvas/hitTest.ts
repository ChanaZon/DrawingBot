import type { Point, SceneObject } from "../types/SceneObject";

// Future: return the topmost object under a point, for selection. Stubbed for now.
export function hitTest(_scene: SceneObject[], _point: Point): SceneObject | null {
  return null;
}
